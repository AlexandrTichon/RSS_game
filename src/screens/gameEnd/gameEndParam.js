import React from 'react';

const message = (
  <div className="yourLose">
    <h3 className="messageHead">Sorry, you can't play without health.</h3>
  </div>
);

const btnParam = 'Reset game';

export { message, btnParam };
